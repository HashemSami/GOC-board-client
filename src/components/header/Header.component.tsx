import { FC } from "react";

import { HeaderContainer } from "./Header.styles";

const Header: FC = () => {
  return (
    <HeaderContainer>
      <h1>GOC board</h1>
    </HeaderContainer>
  );
};

export default Header;
