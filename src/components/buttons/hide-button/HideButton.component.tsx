import { FC } from "react";
import { HideButtonContainer } from "./HideButton.styles";

const HideButton: FC = () => {
  return (
    <HideButtonContainer>
      <button>hide</button>
    </HideButtonContainer>
  );
};

export default HideButton;
